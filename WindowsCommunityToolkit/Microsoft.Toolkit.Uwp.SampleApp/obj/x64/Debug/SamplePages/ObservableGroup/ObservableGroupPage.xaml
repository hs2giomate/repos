<Page x:ConnectionId='1' x:Class="Microsoft.Toolkit.Uwp.SampleApp.SamplePages.ObservableGroupPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:collections="using:Microsoft.Toolkit.Collections"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="using:Microsoft.Toolkit.Uwp.SampleApp.SamplePages"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:system="using:System"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
      mc:Ignorable="d">

    <Page.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="8,0,0,0" />
        </Style>

        <DataTemplate x:Key="PersonDataTemplate"
                                               >
            <TextBlock x:ConnectionId='8'                      />
        </DataTemplate>

        <DataTemplate x:Key="GroupDataTemplate"
                                                                       >
            <StackPanel x:ConnectionId='4' Orientation="Horizontal">
                <TextBlock x:ConnectionId='5' Style="{StaticResource SubtitleTextBlockStyle}"
                                                         />
                <TextBlock x:ConnectionId='6' Margin="8,0,0,0"
                           VerticalAlignment="Bottom"
                           Opacity="0.8"
                           Style="{StaticResource CaptionTextBlockStyle}"
                                                                                                   />
            </StackPanel>
        </DataTemplate>

        <CollectionViewSource x:ConnectionId='2' x:Key="cvs"
                              x:Name="cvs"
                              IsSourceGrouped="True"
                                                         />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Margin="0,4"
                    HorizontalAlignment="Center"
                    Orientation="Horizontal">
            <TextBox x:ConnectionId='10' x:Name="NewContact"
                     MinWidth="200"
                     PlaceholderText="New contact"
                                                          
                                                  />
            <Button x:ConnectionId='11' x:Name="AddContact"
                                             
                    Content="Add"
                    IsEnabled="False" />
            <Button x:ConnectionId='12' x:Name="RemoveContact"
                                               
                    Content="Remove selected"
                    IsEnabled="False" />
        </StackPanel>

        <ListView x:ConnectionId='9' x:Name="ContactsListView"
                  Grid.Row="1"
                  ItemTemplate="{StaticResource PersonDataTemplate}"
                                                 
                                                                       
                  SelectionMode="Single">
            <ListView.GroupStyle>
                <GroupStyle HeaderTemplate="{StaticResource GroupDataTemplate}" />
            </ListView.GroupStyle>
        </ListView>
    </Grid>
</Page>

